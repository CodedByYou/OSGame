package me.codedbyyou.os.client.game.enums

enum class GameState {
    MENU,
    SERVER_JOIN_MENU,
    PLAYING,
    ROUND_OVER,
    LEADERBOARD,
    GAME_OVER
}

enum class GameMode {
    SINGLE_PLAYER,
    MULTI_PLAYER
}